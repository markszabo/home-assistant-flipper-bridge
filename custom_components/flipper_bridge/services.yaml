send_cli:
  name: Send CLI Command
  description: Send a CLI command to the connected Flipper Zero
  fields:
    command:
      name: Command
      description: CLI command to send to Flipper Zero
      example: "help"
      required: true
      selector:
        text:
